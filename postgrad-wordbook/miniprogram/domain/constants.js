const FAMILIARITY = Object.freeze({
  FAMILIAR: 'familiar',
  REVIEW: 'review',
  UNKNOWN: 'unknown',
});

const QUIZ_TYPE = Object.freeze({
  EN_TO_ZH: 'en-to-zh',
  ZH_TO_EN: 'zh-to-en',
});

const DATA_VERSION = 1;

module.exports = {
  FAMILIARITY,
  QUIZ_TYPE,
  DATA_VERSION,
};
